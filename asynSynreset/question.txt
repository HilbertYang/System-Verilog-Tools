4. 异步复位同步释放题

题目：异步复位、同步释放电路

请设计一个复位同步模块：

输入：

clk
arst_n（低有效异步复位）

输出：

srst_n

要求：

外部复位 arst_n 可以异步拉低。
复位解除时，必须在 clk 域内同步释放。
用 2 级触发器实现。
说明为什么“异步断言、同步释放”比“完全异步”更安全。
给出简洁 Verilog 实现。

面试常见追问：

为什么同步释放能降低亚稳态风险？
两级同步器够不够？什么时候可能用三级？
这个电路算不算 CDC？
